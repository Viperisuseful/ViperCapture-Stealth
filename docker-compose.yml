services:
  vipercapture:
    image: ghcr.io/viperisuseful/vipercapture-stealth:0.1.0-beta
    build: .
    init: true
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      VIPERCAPTURE_PUBLIC_URL: ${VIPERCAPTURE_PUBLIC_URL:-http://127.0.0.1:8000}
      VIPERCAPTURE_BROWSER_CHANNEL: ${VIPERCAPTURE_BROWSER_CHANNEL:-chromium}
      VIPERCAPTURE_HEADLESS: ${VIPERCAPTURE_HEADLESS:-1}
      VIPERCAPTURE_MAX_CONCURRENCY: ${VIPERCAPTURE_MAX_CONCURRENCY:-}
      VIPERCAPTURE_BROWSER_RECYCLE_RENDERS: ${VIPERCAPTURE_BROWSER_RECYCLE_RENDERS:-1000}
      VIPERCAPTURE_MAX_PIXELS: ${VIPERCAPTURE_MAX_PIXELS:-500000000}
      VIPERCAPTURE_MAX_WIDTH: ${VIPERCAPTURE_MAX_WIDTH:-16384}
      VIPERCAPTURE_MAX_HEIGHT: ${VIPERCAPTURE_MAX_HEIGHT:-16384}
      VIPERCAPTURE_MAX_FULL_PAGE_HEIGHT: ${VIPERCAPTURE_MAX_FULL_PAGE_HEIGHT:-100000}
      VIPERCAPTURE_MAX_OUTPUT_BYTES: ${VIPERCAPTURE_MAX_OUTPUT_BYTES:-1073741824}
      VIPERCAPTURE_SIGNING_SECRET: ${VIPERCAPTURE_SIGNING_SECRET:-}
      VIPERCAPTURE_SIGNING_ADMIN_TOKEN: ${VIPERCAPTURE_SIGNING_ADMIN_TOKEN:-}
      VIPERCAPTURE_WEBHOOK_SECRET: ${VIPERCAPTURE_WEBHOOK_SECRET:-}
      VIPERCAPTURE_ADMIN_TOKEN: ${VIPERCAPTURE_ADMIN_TOKEN:-}
      VIPERCAPTURE_CONTROL_SECRET: ${VIPERCAPTURE_CONTROL_SECRET:-}
      VIPERCAPTURE_ALLOW_CUSTOM_PROXIES: ${VIPERCAPTURE_ALLOW_CUSTOM_PROXIES:-1}
      VIPERCAPTURE_CAPTCHA_HANDLER_FACTORY: ${VIPERCAPTURE_CAPTCHA_HANDLER_FACTORY:-}
    volumes:
      - vipercapture-data:/data
    shm_size: 512mb
    read_only: true
    tmpfs:
      - /tmp:size=536870912,mode=1777
      - /app/captures:size=67108864,uid=10001,gid=10001,mode=0700
      - /home/vipercapture/.cache:size=67108864,uid=10001,gid=10001,mode=0700
    cap_drop:
      - ALL
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

volumes:
  vipercapture-data:
